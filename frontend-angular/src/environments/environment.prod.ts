export const environment = {
  production: true,
  // Esta variable sera reemplazada por el script de build en el Dockerfile o
  // puedes reemplazarla manualmente con la URL publica de tu ms-gateway en Railway
  apiUrl: 'API_URL_PLACEHOLDER/api'
};
